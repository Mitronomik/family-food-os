{
  "name": "family-food-os-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs dist",
    "build": "tsc -p tsconfig.json && node scripts/build.mjs",
    "preview": "npm run build && node scripts/dev-server.mjs dist",
    "test:frontend-identity": "node --test test/frontend-identity.test.mjs",
    "test:dev-server-identity": "node --test test/dev-server-identity.test.mjs",
    "test:form-validation": "rm -rf dist-tests/form-validation && tsc -p tsconfig.test.form-validation.json && node --test test/form-validation.test.mjs",
    "test:targeted-validation-update": "rm -rf dist-tests/targeted-validation-update && tsc -p tsconfig.test.targeted-validation-update.json && node --test test/targeted-validation-update.test.mjs",
    "test:order-mutation-lifecycle": "rm -rf dist-tests/order-mutation-lifecycle && tsc -p tsconfig.test.order-mutation-lifecycle.json && node --test test/order-mutation-lifecycle.test.mjs",
    "test:order-readiness-presentation": "rm -rf dist-tests/order-readiness-presentation && tsc -p tsconfig.test.order-readiness-presentation.json && node --test test/order-readiness-presentation.test.mjs",
    "test:order-production-feedback": "rm -rf dist-tests/order-production-feedback && tsc -p tsconfig.test.order-production-feedback.json && node --test test/order-production-feedback.test.mjs",
    "test:order-production-context": "rm -rf dist-tests/order-production-context && tsc -p tsconfig.test.order-production-context.json && node --test test/order-production-context.test.mjs",
    "test:production-financial-presentation": "rm -rf dist-tests/production-financial-presentation && tsc -p tsconfig.test.production-financial-presentation.json && node --test test/production-financial-presentation.test.mjs",
    "test:report-financial-presentation": "rm -rf dist-tests/report-financial-presentation && tsc -p tsconfig.test.report-financial-presentation.json && node --test test/report-financial-presentation.test.mjs",
    "test:dashboard-onboarding-feedback": "rm -rf dist-tests/dashboard-onboarding-feedback && tsc -p tsconfig.test.dashboard-onboarding-feedback.json && node --test test/dashboard-onboarding-feedback.test.mjs",
    "test:help-passive-regression": "rm -rf dist-tests/help-passive-regression && tsc -p tsconfig.test.help-passive-regression.json && node --test test/help-passive-regression.test.mjs",
    "test:alerts-feedback": "rm -rf dist-tests/alerts-feedback && tsc -p tsconfig.test.alerts-feedback.json && node --test test/alerts-feedback.test.mjs",
    "test:purchase-suggestions-feedback": "rm -rf dist-tests/purchase-suggestions-feedback && tsc -p tsconfig.test.purchase-suggestions-feedback.json && node --test test/purchase-suggestions-feedback.test.mjs",
    "test:local-artifacts-reports-feedback": "rm -rf dist-tests/local-artifacts-reports-feedback && tsc -p tsconfig.test.local-artifacts-reports-feedback.json && node --test test/local-artifacts-reports-feedback.test.mjs",
    "test:report-document-audit-contract": "rm -rf dist-tests/report-document-audit-contract && tsc -p tsconfig.test.report-document-audit-contract.json && node --test test/report-document-audit-contract.test.mjs",
    "test:export-audit-contract": "rm -rf dist-tests/export-audit-contract && tsc -p tsconfig.test.export-audit-contract.json && node --test test/export-audit-contract.test.mjs",
    "test:backup-audit-contract": "rm -rf dist-tests/backup-audit-contract && tsc -p tsconfig.test.backup-audit-contract.json && node --test test/backup-audit-contract.test.mjs",
    "test:formula-client-workspace-feedback": "rm -rf dist-tests/formula-client-workspace-feedback && tsc -p tsconfig.test.formula-client-workspace-feedback.json && node --test test/formula-client-workspace-feedback.test.mjs",
    "test:inventory-catalog-workspace-feedback": "rm -rf dist-tests/inventory-catalog-workspace-feedback && tsc -p tsconfig.test.inventory-catalog-workspace-feedback.json && node --test test/inventory-catalog-workspace-feedback.test.mjs",
    "test:settings-tax-feedback": "rm -rf dist-tests/settings-tax-feedback && tsc -p tsconfig.test.settings-tax-feedback.json && node --test test/settings-tax-feedback.test.mjs",
    "test:settings-update-status": "rm -rf dist-tests/settings-update-status && tsc -p tsconfig.test.settings-update-status.json && node --test test/settings-update-status.test.mjs",
    "test:audit-log-workspace": "rm -rf dist-tests/audit-log-workspace && tsc -p tsconfig.test.audit-log-workspace.json && node --test test/audit-log-workspace.test.mjs",
    "test:restore-control": "rm -rf dist-tests/restore-control && tsc -p tsconfig.test.restore-control.json && node --test test/restore-control.test.mjs test/restore-control-races.test.mjs",
    "test:core-workspace-feedback": "npm run test:formula-client-workspace-feedback && npm run test:inventory-catalog-workspace-feedback"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
